export default {
    setMenuList(state, key) {
        state.menuList = key
    },
    setActiveHeader(state, key) {
        state.activeHeader = key
    },
    setActiveSider(state, key) {
        state.activeSider = key
    },
    setOpenKey(state, key) {
        state.openKey = key
    },
    setSiderKey(state, key) {
        state.siderKey = key
    },
    setLoginUser(state, key) {
        state.loginUser = key
    },
    setClientIp(state, key) {
        state.clientIp = key
    },
    setAppList(state, key) {
        state.appList = key
    },
}
